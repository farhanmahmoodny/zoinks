<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <br>
<br>
<br>

<div class="row">
   <div class="col s12 m7">
     <div class="card">
       <div class="card-image">
         <img src=<%= @car.image %>>
         <span class="card-title grey darken-3"><%= @car.year.to_s + " " + @car.make + " " + @car.model %></span>
       </div>
       <div class="card-content">
         <p>Color: <%= @car.color %></p>
         <p>Price: <%= @car.r_p %></p>
         <p>Available: <%= @car.available %></p>
         <p>Review:<br><% @car.rentals.each do |rent| %>
           <%= rent.review %>
         <% end %></p>
       </div>
       <div class="card-action">
         <a><% if @car.available == true %>
           <%= link_to "Rent Me", new_rental_path %>
         <% end %></a>
         <a><% if current_user.username == "admin" %>
         <%= link_to "Edit", edit_car_path(@car) %>
         <%= link_to "Delete", @car, method: :delete, data: {confirm: "Are you sure?"} %>
         <% end %></a>
       </div>
     </div>
   </div>
 </div>

  </body>
</html>
