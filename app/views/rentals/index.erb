<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1>Your previous rentals</h1>

    <% @rentals.each do |rent| %>
    <%= rent.car.make + " " + rent.car.model %><br>
    From: <%= rent.pick_up.strftime("%B %d, %Y") %><br>
    Until: <%= rent.drop_off.strftime("%B %d, %Y") %><br>
    <%= link_to "Review", edit_rental_path(rent) %><br><br>
    <% end %>

  </body>
</html>
